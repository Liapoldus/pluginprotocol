// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.4
//   protoc               v7.34.1
// source: liapoldus/plugin/v1/grant.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export const protobufPackage = "liapoldus.plugin.v1";

export interface ActiveGrant {
  handle: string;
  purpose: string;
  domains: string[];
  capability: string;
}

export interface RedeemGrantRequest {
  handle: string;
  purpose: string;
  domain: string;
  capability: string;
}

export interface RedeemGrantResponse {
  secret: Uint8Array;
}

function createBaseActiveGrant(): ActiveGrant {
  return { handle: "", purpose: "", domains: [], capability: "" };
}

export const ActiveGrant: MessageFns<ActiveGrant> = {
  encode(message: ActiveGrant, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.handle !== "") {
      writer.uint32(10).string(message.handle);
    }
    if (message.purpose !== "") {
      writer.uint32(18).string(message.purpose);
    }
    for (const v of message.domains) {
      writer.uint32(26).string(v!);
    }
    if (message.capability !== "") {
      writer.uint32(34).string(message.capability);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ActiveGrant {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseActiveGrant();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.handle = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.purpose = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.domains.push(reader.string());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.capability = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ActiveGrant {
    return {
      handle: isSet(object.handle) ? globalThis.String(object.handle) : "",
      purpose: isSet(object.purpose) ? globalThis.String(object.purpose) : "",
      domains: globalThis.Array.isArray(object?.domains) ? object.domains.map((e: any) => globalThis.String(e)) : [],
      capability: isSet(object.capability) ? globalThis.String(object.capability) : "",
    };
  },

  toJSON(message: ActiveGrant): unknown {
    const obj: any = {};
    if (message.handle !== "") {
      obj.handle = message.handle;
    }
    if (message.purpose !== "") {
      obj.purpose = message.purpose;
    }
    if (message.domains?.length) {
      obj.domains = message.domains;
    }
    if (message.capability !== "") {
      obj.capability = message.capability;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ActiveGrant>, I>>(base?: I): ActiveGrant {
    return ActiveGrant.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ActiveGrant>, I>>(object: I): ActiveGrant {
    const message = createBaseActiveGrant();
    message.handle = object.handle ?? "";
    message.purpose = object.purpose ?? "";
    message.domains = object.domains?.map((e) => e) || [];
    message.capability = object.capability ?? "";
    return message;
  },
};

function createBaseRedeemGrantRequest(): RedeemGrantRequest {
  return { handle: "", purpose: "", domain: "", capability: "" };
}

export const RedeemGrantRequest: MessageFns<RedeemGrantRequest> = {
  encode(message: RedeemGrantRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.handle !== "") {
      writer.uint32(10).string(message.handle);
    }
    if (message.purpose !== "") {
      writer.uint32(18).string(message.purpose);
    }
    if (message.domain !== "") {
      writer.uint32(26).string(message.domain);
    }
    if (message.capability !== "") {
      writer.uint32(34).string(message.capability);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RedeemGrantRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseRedeemGrantRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.handle = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.purpose = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.domain = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.capability = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): RedeemGrantRequest {
    return {
      handle: isSet(object.handle) ? globalThis.String(object.handle) : "",
      purpose: isSet(object.purpose) ? globalThis.String(object.purpose) : "",
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      capability: isSet(object.capability) ? globalThis.String(object.capability) : "",
    };
  },

  toJSON(message: RedeemGrantRequest): unknown {
    const obj: any = {};
    if (message.handle !== "") {
      obj.handle = message.handle;
    }
    if (message.purpose !== "") {
      obj.purpose = message.purpose;
    }
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.capability !== "") {
      obj.capability = message.capability;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RedeemGrantRequest>, I>>(base?: I): RedeemGrantRequest {
    return RedeemGrantRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RedeemGrantRequest>, I>>(object: I): RedeemGrantRequest {
    const message = createBaseRedeemGrantRequest();
    message.handle = object.handle ?? "";
    message.purpose = object.purpose ?? "";
    message.domain = object.domain ?? "";
    message.capability = object.capability ?? "";
    return message;
  },
};

function createBaseRedeemGrantResponse(): RedeemGrantResponse {
  return { secret: new Uint8Array(0) };
}

export const RedeemGrantResponse: MessageFns<RedeemGrantResponse> = {
  encode(message: RedeemGrantResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.secret.length !== 0) {
      writer.uint32(10).bytes(message.secret);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RedeemGrantResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseRedeemGrantResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.secret = reader.bytes();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): RedeemGrantResponse {
    return { secret: isSet(object.secret) ? bytesFromBase64(object.secret) : new Uint8Array(0) };
  },

  toJSON(message: RedeemGrantResponse): unknown {
    const obj: any = {};
    if (message.secret.length !== 0) {
      obj.secret = base64FromBytes(message.secret);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RedeemGrantResponse>, I>>(base?: I): RedeemGrantResponse {
    return RedeemGrantResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RedeemGrantResponse>, I>>(object: I): RedeemGrantResponse {
    const message = createBaseRedeemGrantResponse();
    message.secret = object.secret ?? new Uint8Array(0);
    return message;
  },
};

/**
 * GrantBroker is hosted by Gateway on a private loopback endpoint. Plugins
 * redeem opaque, per-call handles; secret material is never part of Call JSON.
 */
export type GrantBrokerService = typeof GrantBrokerService;
export const GrantBrokerService = {
  redeemGrant: {
    path: "/liapoldus.plugin.v1.GrantBroker/RedeemGrant" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: RedeemGrantRequest): Buffer => Buffer.from(RedeemGrantRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): RedeemGrantRequest => RedeemGrantRequest.decode(value),
    responseSerialize: (value: RedeemGrantResponse): Buffer => Buffer.from(RedeemGrantResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): RedeemGrantResponse => RedeemGrantResponse.decode(value),
  },
} as const;

export interface GrantBrokerServer extends UntypedServiceImplementation {
  redeemGrant: handleUnaryCall<RedeemGrantRequest, RedeemGrantResponse>;
}

export interface GrantBrokerClient extends Client {
  redeemGrant(
    request: RedeemGrantRequest,
    callback: (error: ServiceError | null, response: RedeemGrantResponse) => void,
  ): ClientUnaryCall;
  redeemGrant(
    request: RedeemGrantRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: RedeemGrantResponse) => void,
  ): ClientUnaryCall;
  redeemGrant(
    request: RedeemGrantRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: RedeemGrantResponse) => void,
  ): ClientUnaryCall;
}

export const GrantBrokerClient = makeGenericClientConstructor(
  GrantBrokerService,
  "liapoldus.plugin.v1.GrantBroker",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): GrantBrokerClient;
  service: typeof GrantBrokerService;
  serviceName: string;
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
