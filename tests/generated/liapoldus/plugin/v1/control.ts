// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.4
//   protoc               v7.34.1
// source: liapoldus/plugin/v1/control.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { ActiveGrant } from "./grant.js";

export const protobufPackage = "liapoldus.plugin.v1";

export enum InvocationMode {
  INVOCATION_MODE_UNSPECIFIED = 0,
  INVOCATION_MODE_CALL = 1,
  INVOCATION_MODE_HTTP_STREAM = 2,
  INVOCATION_MODE_WEBSOCKET = 3,
  INVOCATION_MODE_SSE = 4,
  INVOCATION_MODE_TCP = 5,
  INVOCATION_MODE_UDP = 6,
  UNRECOGNIZED = -1,
}

export function invocationModeFromJSON(object: any): InvocationMode {
  switch (object) {
    case 0:
    case "INVOCATION_MODE_UNSPECIFIED":
      return InvocationMode.INVOCATION_MODE_UNSPECIFIED;
    case 1:
    case "INVOCATION_MODE_CALL":
      return InvocationMode.INVOCATION_MODE_CALL;
    case 2:
    case "INVOCATION_MODE_HTTP_STREAM":
      return InvocationMode.INVOCATION_MODE_HTTP_STREAM;
    case 3:
    case "INVOCATION_MODE_WEBSOCKET":
      return InvocationMode.INVOCATION_MODE_WEBSOCKET;
    case 4:
    case "INVOCATION_MODE_SSE":
      return InvocationMode.INVOCATION_MODE_SSE;
    case 5:
    case "INVOCATION_MODE_TCP":
      return InvocationMode.INVOCATION_MODE_TCP;
    case 6:
    case "INVOCATION_MODE_UDP":
      return InvocationMode.INVOCATION_MODE_UDP;
    case -1:
    case "UNRECOGNIZED":
    default:
      return InvocationMode.UNRECOGNIZED;
  }
}

export function invocationModeToJSON(object: InvocationMode): string {
  switch (object) {
    case InvocationMode.INVOCATION_MODE_UNSPECIFIED:
      return "INVOCATION_MODE_UNSPECIFIED";
    case InvocationMode.INVOCATION_MODE_CALL:
      return "INVOCATION_MODE_CALL";
    case InvocationMode.INVOCATION_MODE_HTTP_STREAM:
      return "INVOCATION_MODE_HTTP_STREAM";
    case InvocationMode.INVOCATION_MODE_WEBSOCKET:
      return "INVOCATION_MODE_WEBSOCKET";
    case InvocationMode.INVOCATION_MODE_SSE:
      return "INVOCATION_MODE_SSE";
    case InvocationMode.INVOCATION_MODE_TCP:
      return "INVOCATION_MODE_TCP";
    case InvocationMode.INVOCATION_MODE_UDP:
      return "INVOCATION_MODE_UDP";
    case InvocationMode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface ManifestRequest {
}

/**
 * Bootstrap carries only operational connection information supplied by the
 * Gateway. It must never contain plugin settings or secret material.
 */
export interface BootstrapRequest {
  instanceId: string;
  grantBrokerEndpoint: string;
}

export interface BootstrapResult {
  accepted: boolean;
}

export interface Manifest {
  name: string;
  capabilities: string[];
  protocolVersion: string;
  capabilityDescriptors: CapabilityDescriptor[];
}

export interface CapabilityDescriptor {
  capability: string;
  modes: InvocationMode[];
}

/**
 * DispatchApply replaces the complete active data-plane authorization scope
 * for one logical plugin instance on the connected replica. Applying a
 * request is atomic: on failure, the previous generation remains active.
 * Generations start at 1 and increase monotonically per replica. An identical
 * request repeated at the current generation is idempotent; a stale generation
 * or a different request at the current generation is rejected.
 */
export interface DispatchApplyRequest {
  generation: number;
  instanceId: string;
  settingsDigest: string;
  releaseDigest: string;
  capabilities: CapabilityDispatchScope[];
}

/**
 * Each capability is unique in one request and has one or more unique,
 * non-unspecified modes declared by the replica's Manifest. An empty
 * capabilities list installs deny-all for this generation.
 */
export interface CapabilityDispatchScope {
  capability: string;
  modes: InvocationMode[];
}

/**
 * A successful response is emitted only after this replica has installed the
 * requested generation. replica_identity_uri identifies the connected remote
 * replica and must match its URI SAN as verified by the Gateway TLS peer.
 * Settings and release digests acknowledge the replica-local active values.
 * dispatch_digest is a non-empty opaque identifier for the installed complete
 * capability-to-mode scope; v1 does not standardize its encoding.
 */
export interface DispatchApplyResponse {
  generation: number;
  replicaIdentityUri: string;
  manifestDigest: string;
  settingsDigest: string;
  releaseDigest: string;
  dispatchDigest: string;
}

export interface ConfigSchemaRequest {
}

export interface ConfigField {
  name: string;
  type: string;
  required: boolean;
  defaultJson: string;
  options: string[];
  description: string;
}

export interface ConfigSchema {
  fields: ConfigField[];
}

/**
 * ConfigApply is a Gateway-pushed, atomic settings revision. config is the
 * versioned JSON document with opaque references only; grants contain no
 * secret bytes and are valid only for this instance/revision/reference.
 */
export interface ConfigApplyRequest {
  config: Uint8Array;
  settingsRevision: string;
  grants: ActiveGrant[];
}

/**
 * A plugin acknowledges the exact revision only after applying settings and
 * resolving any required config-scoped grants in memory.
 */
export interface ConfigApplyResult {
  applied: boolean;
  settingsRevision: string;
}

export interface ShutdownRequest {
}

export interface ShutdownResult {
  closed: boolean;
}

function createBaseManifestRequest(): ManifestRequest {
  return {};
}

export const ManifestRequest: MessageFns<ManifestRequest> = {
  encode(_: ManifestRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ManifestRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseManifestRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(_: any): ManifestRequest {
    return {};
  },

  toJSON(_: ManifestRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ManifestRequest>, I>>(base?: I): ManifestRequest {
    return ManifestRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ManifestRequest>, I>>(_: I): ManifestRequest {
    const message = createBaseManifestRequest();
    return message;
  },
};

function createBaseBootstrapRequest(): BootstrapRequest {
  return { instanceId: "", grantBrokerEndpoint: "" };
}

export const BootstrapRequest: MessageFns<BootstrapRequest> = {
  encode(message: BootstrapRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.instanceId !== "") {
      writer.uint32(10).string(message.instanceId);
    }
    if (message.grantBrokerEndpoint !== "") {
      writer.uint32(18).string(message.grantBrokerEndpoint);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BootstrapRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseBootstrapRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.instanceId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.grantBrokerEndpoint = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): BootstrapRequest {
    return {
      instanceId: isSet(object.instanceId)
        ? globalThis.String(object.instanceId)
        : isSet(object.instance_id)
        ? globalThis.String(object.instance_id)
        : "",
      grantBrokerEndpoint: isSet(object.grantBrokerEndpoint)
        ? globalThis.String(object.grantBrokerEndpoint)
        : isSet(object.grant_broker_endpoint)
        ? globalThis.String(object.grant_broker_endpoint)
        : "",
    };
  },

  toJSON(message: BootstrapRequest): unknown {
    const obj: any = {};
    if (message.instanceId !== "") {
      obj.instanceId = message.instanceId;
    }
    if (message.grantBrokerEndpoint !== "") {
      obj.grantBrokerEndpoint = message.grantBrokerEndpoint;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BootstrapRequest>, I>>(base?: I): BootstrapRequest {
    return BootstrapRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BootstrapRequest>, I>>(object: I): BootstrapRequest {
    const message = createBaseBootstrapRequest();
    message.instanceId = object.instanceId ?? "";
    message.grantBrokerEndpoint = object.grantBrokerEndpoint ?? "";
    return message;
  },
};

function createBaseBootstrapResult(): BootstrapResult {
  return { accepted: false };
}

export const BootstrapResult: MessageFns<BootstrapResult> = {
  encode(message: BootstrapResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accepted !== false) {
      writer.uint32(8).bool(message.accepted);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BootstrapResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseBootstrapResult();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.accepted = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): BootstrapResult {
    return { accepted: isSet(object.accepted) ? globalThis.Boolean(object.accepted) : false };
  },

  toJSON(message: BootstrapResult): unknown {
    const obj: any = {};
    if (message.accepted !== false) {
      obj.accepted = message.accepted;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BootstrapResult>, I>>(base?: I): BootstrapResult {
    return BootstrapResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BootstrapResult>, I>>(object: I): BootstrapResult {
    const message = createBaseBootstrapResult();
    message.accepted = object.accepted ?? false;
    return message;
  },
};

function createBaseManifest(): Manifest {
  return { name: "", capabilities: [], protocolVersion: "", capabilityDescriptors: [] };
}

export const Manifest: MessageFns<Manifest> = {
  encode(message: Manifest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    for (const v of message.capabilities) {
      writer.uint32(18).string(v!);
    }
    if (message.protocolVersion !== "") {
      writer.uint32(26).string(message.protocolVersion);
    }
    for (const v of message.capabilityDescriptors) {
      CapabilityDescriptor.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Manifest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseManifest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.name = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.capabilities.push(reader.string());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.protocolVersion = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.capabilityDescriptors.push(CapabilityDescriptor.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Manifest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      capabilities: globalThis.Array.isArray(object?.capabilities)
        ? object.capabilities.map((e: any) => globalThis.String(e))
        : [],
      protocolVersion: isSet(object.protocolVersion)
        ? globalThis.String(object.protocolVersion)
        : isSet(object.protocol_version)
        ? globalThis.String(object.protocol_version)
        : "",
      capabilityDescriptors: globalThis.Array.isArray(object?.capabilityDescriptors)
        ? object.capabilityDescriptors.map((e: any) => CapabilityDescriptor.fromJSON(e))
        : globalThis.Array.isArray(object?.capability_descriptors)
        ? object.capability_descriptors.map((e: any) => CapabilityDescriptor.fromJSON(e))
        : [],
    };
  },

  toJSON(message: Manifest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.capabilities?.length) {
      obj.capabilities = message.capabilities;
    }
    if (message.protocolVersion !== "") {
      obj.protocolVersion = message.protocolVersion;
    }
    if (message.capabilityDescriptors?.length) {
      obj.capabilityDescriptors = message.capabilityDescriptors.map((e) => CapabilityDescriptor.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Manifest>, I>>(base?: I): Manifest {
    return Manifest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Manifest>, I>>(object: I): Manifest {
    const message = createBaseManifest();
    message.name = object.name ?? "";
    message.capabilities = object.capabilities?.map((e) => e) || [];
    message.protocolVersion = object.protocolVersion ?? "";
    message.capabilityDescriptors = object.capabilityDescriptors?.map((e) => CapabilityDescriptor.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCapabilityDescriptor(): CapabilityDescriptor {
  return { capability: "", modes: [] };
}

export const CapabilityDescriptor: MessageFns<CapabilityDescriptor> = {
  encode(message: CapabilityDescriptor, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.capability !== "") {
      writer.uint32(10).string(message.capability);
    }
    writer.uint32(18).fork();
    for (const v of message.modes) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CapabilityDescriptor {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCapabilityDescriptor();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.capability = reader.string();
            continue;
          }
          case 2: {
            if (tag === 16) {
              message.modes.push(reader.int32() as any);

              continue;
            }

            if (tag === 18) {
              const end2 = reader.uint32() + reader.pos;
              while (reader.pos < end2) {
                message.modes.push(reader.int32() as any);
              }

              continue;
            }

            break;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): CapabilityDescriptor {
    return {
      capability: isSet(object.capability) ? globalThis.String(object.capability) : "",
      modes: globalThis.Array.isArray(object?.modes) ? object.modes.map((e: any) => invocationModeFromJSON(e)) : [],
    };
  },

  toJSON(message: CapabilityDescriptor): unknown {
    const obj: any = {};
    if (message.capability !== "") {
      obj.capability = message.capability;
    }
    if (message.modes?.length) {
      obj.modes = message.modes.map((e) => invocationModeToJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CapabilityDescriptor>, I>>(base?: I): CapabilityDescriptor {
    return CapabilityDescriptor.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CapabilityDescriptor>, I>>(object: I): CapabilityDescriptor {
    const message = createBaseCapabilityDescriptor();
    message.capability = object.capability ?? "";
    message.modes = object.modes?.map((e) => e) || [];
    return message;
  },
};

function createBaseDispatchApplyRequest(): DispatchApplyRequest {
  return { generation: 0, instanceId: "", settingsDigest: "", releaseDigest: "", capabilities: [] };
}

export const DispatchApplyRequest: MessageFns<DispatchApplyRequest> = {
  encode(message: DispatchApplyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.generation !== 0) {
      writer.uint32(8).uint64(message.generation);
    }
    if (message.instanceId !== "") {
      writer.uint32(18).string(message.instanceId);
    }
    if (message.settingsDigest !== "") {
      writer.uint32(26).string(message.settingsDigest);
    }
    if (message.releaseDigest !== "") {
      writer.uint32(34).string(message.releaseDigest);
    }
    for (const v of message.capabilities) {
      CapabilityDispatchScope.encode(v!, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DispatchApplyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseDispatchApplyRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.generation = longToNumber(reader.uint64());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.instanceId = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.settingsDigest = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.releaseDigest = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.capabilities.push(CapabilityDispatchScope.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): DispatchApplyRequest {
    return {
      generation: isSet(object.generation) ? globalThis.Number(object.generation) : 0,
      instanceId: isSet(object.instanceId)
        ? globalThis.String(object.instanceId)
        : isSet(object.instance_id)
        ? globalThis.String(object.instance_id)
        : "",
      settingsDigest: isSet(object.settingsDigest)
        ? globalThis.String(object.settingsDigest)
        : isSet(object.settings_digest)
        ? globalThis.String(object.settings_digest)
        : "",
      releaseDigest: isSet(object.releaseDigest)
        ? globalThis.String(object.releaseDigest)
        : isSet(object.release_digest)
        ? globalThis.String(object.release_digest)
        : "",
      capabilities: globalThis.Array.isArray(object?.capabilities)
        ? object.capabilities.map((e: any) => CapabilityDispatchScope.fromJSON(e))
        : [],
    };
  },

  toJSON(message: DispatchApplyRequest): unknown {
    const obj: any = {};
    if (message.generation !== 0) {
      obj.generation = Math.round(message.generation);
    }
    if (message.instanceId !== "") {
      obj.instanceId = message.instanceId;
    }
    if (message.settingsDigest !== "") {
      obj.settingsDigest = message.settingsDigest;
    }
    if (message.releaseDigest !== "") {
      obj.releaseDigest = message.releaseDigest;
    }
    if (message.capabilities?.length) {
      obj.capabilities = message.capabilities.map((e) => CapabilityDispatchScope.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DispatchApplyRequest>, I>>(base?: I): DispatchApplyRequest {
    return DispatchApplyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DispatchApplyRequest>, I>>(object: I): DispatchApplyRequest {
    const message = createBaseDispatchApplyRequest();
    message.generation = object.generation ?? 0;
    message.instanceId = object.instanceId ?? "";
    message.settingsDigest = object.settingsDigest ?? "";
    message.releaseDigest = object.releaseDigest ?? "";
    message.capabilities = object.capabilities?.map((e) => CapabilityDispatchScope.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCapabilityDispatchScope(): CapabilityDispatchScope {
  return { capability: "", modes: [] };
}

export const CapabilityDispatchScope: MessageFns<CapabilityDispatchScope> = {
  encode(message: CapabilityDispatchScope, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.capability !== "") {
      writer.uint32(10).string(message.capability);
    }
    writer.uint32(18).fork();
    for (const v of message.modes) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CapabilityDispatchScope {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCapabilityDispatchScope();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.capability = reader.string();
            continue;
          }
          case 2: {
            if (tag === 16) {
              message.modes.push(reader.int32() as any);

              continue;
            }

            if (tag === 18) {
              const end2 = reader.uint32() + reader.pos;
              while (reader.pos < end2) {
                message.modes.push(reader.int32() as any);
              }

              continue;
            }

            break;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): CapabilityDispatchScope {
    return {
      capability: isSet(object.capability) ? globalThis.String(object.capability) : "",
      modes: globalThis.Array.isArray(object?.modes) ? object.modes.map((e: any) => invocationModeFromJSON(e)) : [],
    };
  },

  toJSON(message: CapabilityDispatchScope): unknown {
    const obj: any = {};
    if (message.capability !== "") {
      obj.capability = message.capability;
    }
    if (message.modes?.length) {
      obj.modes = message.modes.map((e) => invocationModeToJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CapabilityDispatchScope>, I>>(base?: I): CapabilityDispatchScope {
    return CapabilityDispatchScope.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CapabilityDispatchScope>, I>>(object: I): CapabilityDispatchScope {
    const message = createBaseCapabilityDispatchScope();
    message.capability = object.capability ?? "";
    message.modes = object.modes?.map((e) => e) || [];
    return message;
  },
};

function createBaseDispatchApplyResponse(): DispatchApplyResponse {
  return {
    generation: 0,
    replicaIdentityUri: "",
    manifestDigest: "",
    settingsDigest: "",
    releaseDigest: "",
    dispatchDigest: "",
  };
}

export const DispatchApplyResponse: MessageFns<DispatchApplyResponse> = {
  encode(message: DispatchApplyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.generation !== 0) {
      writer.uint32(8).uint64(message.generation);
    }
    if (message.replicaIdentityUri !== "") {
      writer.uint32(18).string(message.replicaIdentityUri);
    }
    if (message.manifestDigest !== "") {
      writer.uint32(26).string(message.manifestDigest);
    }
    if (message.settingsDigest !== "") {
      writer.uint32(34).string(message.settingsDigest);
    }
    if (message.releaseDigest !== "") {
      writer.uint32(42).string(message.releaseDigest);
    }
    if (message.dispatchDigest !== "") {
      writer.uint32(50).string(message.dispatchDigest);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DispatchApplyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseDispatchApplyResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.generation = longToNumber(reader.uint64());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.replicaIdentityUri = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.manifestDigest = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.settingsDigest = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.releaseDigest = reader.string();
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.dispatchDigest = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): DispatchApplyResponse {
    return {
      generation: isSet(object.generation) ? globalThis.Number(object.generation) : 0,
      replicaIdentityUri: isSet(object.replicaIdentityUri)
        ? globalThis.String(object.replicaIdentityUri)
        : isSet(object.replica_identity_uri)
        ? globalThis.String(object.replica_identity_uri)
        : "",
      manifestDigest: isSet(object.manifestDigest)
        ? globalThis.String(object.manifestDigest)
        : isSet(object.manifest_digest)
        ? globalThis.String(object.manifest_digest)
        : "",
      settingsDigest: isSet(object.settingsDigest)
        ? globalThis.String(object.settingsDigest)
        : isSet(object.settings_digest)
        ? globalThis.String(object.settings_digest)
        : "",
      releaseDigest: isSet(object.releaseDigest)
        ? globalThis.String(object.releaseDigest)
        : isSet(object.release_digest)
        ? globalThis.String(object.release_digest)
        : "",
      dispatchDigest: isSet(object.dispatchDigest)
        ? globalThis.String(object.dispatchDigest)
        : isSet(object.dispatch_digest)
        ? globalThis.String(object.dispatch_digest)
        : "",
    };
  },

  toJSON(message: DispatchApplyResponse): unknown {
    const obj: any = {};
    if (message.generation !== 0) {
      obj.generation = Math.round(message.generation);
    }
    if (message.replicaIdentityUri !== "") {
      obj.replicaIdentityUri = message.replicaIdentityUri;
    }
    if (message.manifestDigest !== "") {
      obj.manifestDigest = message.manifestDigest;
    }
    if (message.settingsDigest !== "") {
      obj.settingsDigest = message.settingsDigest;
    }
    if (message.releaseDigest !== "") {
      obj.releaseDigest = message.releaseDigest;
    }
    if (message.dispatchDigest !== "") {
      obj.dispatchDigest = message.dispatchDigest;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DispatchApplyResponse>, I>>(base?: I): DispatchApplyResponse {
    return DispatchApplyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DispatchApplyResponse>, I>>(object: I): DispatchApplyResponse {
    const message = createBaseDispatchApplyResponse();
    message.generation = object.generation ?? 0;
    message.replicaIdentityUri = object.replicaIdentityUri ?? "";
    message.manifestDigest = object.manifestDigest ?? "";
    message.settingsDigest = object.settingsDigest ?? "";
    message.releaseDigest = object.releaseDigest ?? "";
    message.dispatchDigest = object.dispatchDigest ?? "";
    return message;
  },
};

function createBaseConfigSchemaRequest(): ConfigSchemaRequest {
  return {};
}

export const ConfigSchemaRequest: MessageFns<ConfigSchemaRequest> = {
  encode(_: ConfigSchemaRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfigSchemaRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseConfigSchemaRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(_: any): ConfigSchemaRequest {
    return {};
  },

  toJSON(_: ConfigSchemaRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ConfigSchemaRequest>, I>>(base?: I): ConfigSchemaRequest {
    return ConfigSchemaRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConfigSchemaRequest>, I>>(_: I): ConfigSchemaRequest {
    const message = createBaseConfigSchemaRequest();
    return message;
  },
};

function createBaseConfigField(): ConfigField {
  return { name: "", type: "", required: false, defaultJson: "", options: [], description: "" };
}

export const ConfigField: MessageFns<ConfigField> = {
  encode(message: ConfigField, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.type !== "") {
      writer.uint32(18).string(message.type);
    }
    if (message.required !== false) {
      writer.uint32(24).bool(message.required);
    }
    if (message.defaultJson !== "") {
      writer.uint32(34).string(message.defaultJson);
    }
    for (const v of message.options) {
      writer.uint32(42).string(v!);
    }
    if (message.description !== "") {
      writer.uint32(50).string(message.description);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfigField {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseConfigField();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.name = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.type = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.required = reader.bool();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.defaultJson = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.options.push(reader.string());
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.description = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ConfigField {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      required: isSet(object.required) ? globalThis.Boolean(object.required) : false,
      defaultJson: isSet(object.defaultJson)
        ? globalThis.String(object.defaultJson)
        : isSet(object.default_json)
        ? globalThis.String(object.default_json)
        : "",
      options: globalThis.Array.isArray(object?.options) ? object.options.map((e: any) => globalThis.String(e)) : [],
      description: isSet(object.description) ? globalThis.String(object.description) : "",
    };
  },

  toJSON(message: ConfigField): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.required !== false) {
      obj.required = message.required;
    }
    if (message.defaultJson !== "") {
      obj.defaultJson = message.defaultJson;
    }
    if (message.options?.length) {
      obj.options = message.options;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConfigField>, I>>(base?: I): ConfigField {
    return ConfigField.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConfigField>, I>>(object: I): ConfigField {
    const message = createBaseConfigField();
    message.name = object.name ?? "";
    message.type = object.type ?? "";
    message.required = object.required ?? false;
    message.defaultJson = object.defaultJson ?? "";
    message.options = object.options?.map((e) => e) || [];
    message.description = object.description ?? "";
    return message;
  },
};

function createBaseConfigSchema(): ConfigSchema {
  return { fields: [] };
}

export const ConfigSchema: MessageFns<ConfigSchema> = {
  encode(message: ConfigSchema, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.fields) {
      ConfigField.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfigSchema {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseConfigSchema();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.fields.push(ConfigField.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ConfigSchema {
    return {
      fields: globalThis.Array.isArray(object?.fields) ? object.fields.map((e: any) => ConfigField.fromJSON(e)) : [],
    };
  },

  toJSON(message: ConfigSchema): unknown {
    const obj: any = {};
    if (message.fields?.length) {
      obj.fields = message.fields.map((e) => ConfigField.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConfigSchema>, I>>(base?: I): ConfigSchema {
    return ConfigSchema.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConfigSchema>, I>>(object: I): ConfigSchema {
    const message = createBaseConfigSchema();
    message.fields = object.fields?.map((e) => ConfigField.fromPartial(e)) || [];
    return message;
  },
};

function createBaseConfigApplyRequest(): ConfigApplyRequest {
  return { config: new Uint8Array(0), settingsRevision: "", grants: [] };
}

export const ConfigApplyRequest: MessageFns<ConfigApplyRequest> = {
  encode(message: ConfigApplyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.config.length !== 0) {
      writer.uint32(10).bytes(message.config);
    }
    if (message.settingsRevision !== "") {
      writer.uint32(18).string(message.settingsRevision);
    }
    for (const v of message.grants) {
      ActiveGrant.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfigApplyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseConfigApplyRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.config = reader.bytes();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.settingsRevision = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.grants.push(ActiveGrant.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ConfigApplyRequest {
    return {
      config: isSet(object.config) ? bytesFromBase64(object.config) : new Uint8Array(0),
      settingsRevision: isSet(object.settingsRevision)
        ? globalThis.String(object.settingsRevision)
        : isSet(object.settings_revision)
        ? globalThis.String(object.settings_revision)
        : "",
      grants: globalThis.Array.isArray(object?.grants) ? object.grants.map((e: any) => ActiveGrant.fromJSON(e)) : [],
    };
  },

  toJSON(message: ConfigApplyRequest): unknown {
    const obj: any = {};
    if (message.config.length !== 0) {
      obj.config = base64FromBytes(message.config);
    }
    if (message.settingsRevision !== "") {
      obj.settingsRevision = message.settingsRevision;
    }
    if (message.grants?.length) {
      obj.grants = message.grants.map((e) => ActiveGrant.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConfigApplyRequest>, I>>(base?: I): ConfigApplyRequest {
    return ConfigApplyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConfigApplyRequest>, I>>(object: I): ConfigApplyRequest {
    const message = createBaseConfigApplyRequest();
    message.config = object.config ?? new Uint8Array(0);
    message.settingsRevision = object.settingsRevision ?? "";
    message.grants = object.grants?.map((e) => ActiveGrant.fromPartial(e)) || [];
    return message;
  },
};

function createBaseConfigApplyResult(): ConfigApplyResult {
  return { applied: false, settingsRevision: "" };
}

export const ConfigApplyResult: MessageFns<ConfigApplyResult> = {
  encode(message: ConfigApplyResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.applied !== false) {
      writer.uint32(8).bool(message.applied);
    }
    if (message.settingsRevision !== "") {
      writer.uint32(18).string(message.settingsRevision);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfigApplyResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseConfigApplyResult();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.applied = reader.bool();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.settingsRevision = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ConfigApplyResult {
    return {
      applied: isSet(object.applied) ? globalThis.Boolean(object.applied) : false,
      settingsRevision: isSet(object.settingsRevision)
        ? globalThis.String(object.settingsRevision)
        : isSet(object.settings_revision)
        ? globalThis.String(object.settings_revision)
        : "",
    };
  },

  toJSON(message: ConfigApplyResult): unknown {
    const obj: any = {};
    if (message.applied !== false) {
      obj.applied = message.applied;
    }
    if (message.settingsRevision !== "") {
      obj.settingsRevision = message.settingsRevision;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConfigApplyResult>, I>>(base?: I): ConfigApplyResult {
    return ConfigApplyResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConfigApplyResult>, I>>(object: I): ConfigApplyResult {
    const message = createBaseConfigApplyResult();
    message.applied = object.applied ?? false;
    message.settingsRevision = object.settingsRevision ?? "";
    return message;
  },
};

function createBaseShutdownRequest(): ShutdownRequest {
  return {};
}

export const ShutdownRequest: MessageFns<ShutdownRequest> = {
  encode(_: ShutdownRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ShutdownRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseShutdownRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(_: any): ShutdownRequest {
    return {};
  },

  toJSON(_: ShutdownRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ShutdownRequest>, I>>(base?: I): ShutdownRequest {
    return ShutdownRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ShutdownRequest>, I>>(_: I): ShutdownRequest {
    const message = createBaseShutdownRequest();
    return message;
  },
};

function createBaseShutdownResult(): ShutdownResult {
  return { closed: false };
}

export const ShutdownResult: MessageFns<ShutdownResult> = {
  encode(message: ShutdownResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.closed !== false) {
      writer.uint32(8).bool(message.closed);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ShutdownResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseShutdownResult();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.closed = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ShutdownResult {
    return { closed: isSet(object.closed) ? globalThis.Boolean(object.closed) : false };
  },

  toJSON(message: ShutdownResult): unknown {
    const obj: any = {};
    if (message.closed !== false) {
      obj.closed = message.closed;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ShutdownResult>, I>>(base?: I): ShutdownResult {
    return ShutdownResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ShutdownResult>, I>>(object: I): ShutdownResult {
    const message = createBaseShutdownResult();
    message.closed = object.closed ?? false;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
