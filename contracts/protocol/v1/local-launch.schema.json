{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Liapoldus/pluginprotocol/contracts/protocol/v1/local-launch.schema.json",
  "title": "Plugin v1 local process executable",
  "description": "The Gateway starts the executable without application arguments or environment configuration. Operational connection bootstrap is delivered over the typed Bootstrap RPC; application settings are pushed over ConfigApply.",
  "type": "object",
  "additionalProperties": false,
  "required": ["binary"],
  "properties": {
    "binary": {
      "type": "string",
      "pattern": "^/",
      "description": "Absolute path to the plugin executable on the Gateway host."
    }
  }
}
